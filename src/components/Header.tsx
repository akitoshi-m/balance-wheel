import { Typography } from "@mui/material"

const Header = () => {
  return (
    <Typography variant="h4" sx={{p: 2, mb: 2, fontWeight: 'bold', backgroundColor: '#3498DB', color: '#fff' }}>
    人生の輪 作成ツール
    </Typography>
  );
}

export default Header;
